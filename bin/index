#!/usr/bin/env node

/**
 * NOTE: The version of V8 that ships with electron (presently)
 * doesn't support async functions so we just wrap the actual
 * script in an async transpiling require call... babel is also
 * used to transpile JSX.
 */
require('babel-register')({
  plugins: ['transform-react-jsx', 'transform-async-functions'],
  extensions: ['.js'],
  cache: true,
});

/**
 * Execute the actual script.
 */
require('./_index.js');
